name: README Update Schedule Action
on: 
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0 9 * * *'
  push:
  
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v1.7.2
        with:
          dotnet-version: '5.0.x'
      - run: dotnet run --project ./dotnet
      - name: Commit and push changes
        run: |
          git config --global user.name "kasp470f"
          git config --global user.email "11627@student.eadania.dk"

          git add -A
          git commit -m "Daily Update to README.md Language Statistics Table" -m "Sent a daily request to the WakaTime JSON API. The README section has now been updated with new data. This action has been triggered by a ${{ github.event_name }} event"
          git push
